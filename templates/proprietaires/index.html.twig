{% extends 'base.html.twig' %}

{% block title %}Un Propriétaire de la catégorie   {#  {{ categorie.titre }} #}{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="container">
        <h1>proprietaires de la catégorie   {#  {{ categorie.titre }} #} </h1>
       {#  <div class="row">
            {% for p in proprietaires %}
                <div class="col col-3">
                    <div class="card"><img src="{{ p.photo }}" alt=""
                                           class="card-img-top">
                        <div class="card-body">
                            <h2 class="card-title">{{ p.nom }}</h2>
                        </div>
                        <div class="card-body">
                            <h2 class="card-title">{{ chat.nom }}</h2>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
        #}
        <a href="{{ path("app_proprietaires_ajouter") }}" class="mt-5 btn btn-primary">Ajouter un proprietaires</a>
    </div>
{% endblock %}
